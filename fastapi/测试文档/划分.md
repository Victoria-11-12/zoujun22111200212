# FastAPI 项目测试划分文档

## 项目结构概览

```
app/
├── models.py              # 数据模型层 (Pydantic)
├── history.py             # 会话历史管理
├── logs.py                # 日志记录模块
├── config.py              # 全局配置
├── tools/                 # 工具层
│   ├── sql_tools.py       # SQL查询工具
│   ├── admin_tools.py     # 管理员工具集
│   └── web_tools.py       # 网络爬虫工具
├── chains/                # LangChain链层
│   ├── user_chains.py     # 用户对话链
│   ├── admin_chains.py    # 管理员对话链
│   ├── chart_chains.py    # 图表生成链
│   └── eval_chains.py     # 评估链
├── agents/                # Agent层
│   ├── sql_agent.py       # SQL查询Agent
│   └── admin_agent.py     # 管理员Agent
├── workflows/             # 工作流层
│   └── chart_workflow.py  # 图表生成工作流
├── services/              # 服务层
│   ├── user_service.py    # 用户服务
│   ├── admin_service.py   # 管理员服务
│   ├── chart_service.py   # 图表服务
│   └── analyst_service.py # 评估服务
└── routers/               # 路由层
    ├── user.py            # 用户路由
    ├── admin.py           # 管理员路由
    ├── chart.py           # 图表路由
    └── analyst.py         # 评估路由
```

---

## 一、单元测试 (Unit Tests)

### 1.1 数据模型层 (Models)

**文件**: `app/models.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| ChatRequest | 字段验证、默认值、必填校验 | 单元测试 |
| AdminChatRequest | 字段验证、默认值、必填校验 | 单元测试 |
| ChartRequest | 字段验证、默认值、必填校验 | 单元测试 |
| EvaluateRequest | 字段验证、默认值 | 单元测试 |
| EvalQueryRequest | 字段验证、可选字段处理 | 单元测试 |
| ResponseEvalResult | 字段范围验证(1-5分)、类型检查 | 单元测试 |
| CodeEvalResult | 字段范围验证(1-5分)、类型检查 | 单元测试 |

### 1.2 工具函数层 (Tools)

**文件**: `app/tools/sql_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| sql_db_query | SQL查询执行、参数传递 | 单元测试(需Mock数据库) |

**文件**: `app/tools/admin_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| DANGEROUS_KEYWORDS | 列表非空、正则可编译、覆盖DDL关键词和注入模式 | 单元测试 |
| check_sql_safety | 危险关键词检测、正则匹配；注释符`--`在SQL中间位置检测；分号后跟关键字`;\s*\w`匹配；大小写混合绕过尝试；安全SQL应通过 | 单元测试 |
| backup_data | 数据备份逻辑、JSON序列化 | 单元测试(需Mock数据库) |
| create_user | 用户创建、密码加密、重复检测 | 单元测试(需Mock数据库) |
| safe_execute_sql | SQL执行、安全拦截、备份触发；SELECT结果为空时返回值；UPDATE/DELETE无WHERE子句时行为；表名/条件正则匹配失败时行为 | 单元测试(需Mock数据库) |
| start_batch | 批次ID生成、全局变量设置 | 单元测试 |
| rollback_batch | 回滚逻辑、数据恢复；无可回滚记录时返回；混合INSERT/UPDATE/DELETE操作回滚顺序；回滚日志清理验证 | 单元测试(需Mock数据库) |
| set_current_admin_name | 全局变量设置 | 单元测试 |
| admin_tools | 列表完整性验证(4项工具)、工具名称正确 | 单元测试 |

**文件**: `app/tools/web_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| run_agent_command | 子进程调用、超时处理、输出解析 | 单元测试(需Mock subprocess) |
| baike_search_tool | 输入校验、命令组装、结果解析 | 单元测试(需Mock子进程) |

### 1.3 历史管理模块 (History)

**文件**: `app/history.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| conversation_history | 全局字典初始化 | 单元测试 |
| MAX_HISTORY | 常量值验证 | 单元测试 |
| get_history | 存在/不存在会话ID的处理 | 单元测试 |
| save_history | 消息追加、历史截断、LangChain消息封装；恰好等于MAX_HISTORY*2时不截断；超过1条时截断至MAX_HISTORY*2；空字符串消息处理 | 单元测试 |

### 1.4 日志记录模块 (Logs)

**文件**: `app/logs.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| get_db_connection | 连接池获取、上下文管理器 | 单元测试(需Mock连接池) |
| log_user_chat | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_admin_chat | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_chart_generation | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_security_warning | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |

### 1.5 配置模块 (Config)

**文件**: `app/config.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| llm | ChatOpenAI初始化参数 | 单元测试(需Mock环境变量) |
| db | SQLDatabase初始化 | 单元测试(需Mock环境变量) |
| db_user | 只读数据库初始化 | 单元测试(需Mock环境变量) |
| engine | 连接池参数配置 | 单元测试(需Mock环境变量) |
| engine_readonly | 只读连接池配置 | 单元测试(需Mock环境变量) |
| eval_llm | 评估专用LLM初始化 | 单元测试(需Mock环境变量) |
| engine_analyst | 分析师连接池配置 | 单元测试(需Mock环境变量) |
| MODEL_NAME | 环境变量读取、日志依赖验证 | 单元测试(需Mock环境变量) |

### 1.6 工作流纯函数层 (Workflow Pure Functions)

**文件**: `app/workflows/chart_workflow.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| ChartGraphState | 状态定义、类型检查 | 单元测试 |
| _extract_python_code_block | 代码提取正则；无markdown标记时回退到完整文本；多个代码块时只取第一个 | 单元测试 |
| _static_eval | 静态代码检查、安全检测；`from pyecharts.xxx import yyy`形式检测；banned_tokens部分匹配误判；合法pyecharts代码应通过 | 单元测试 |
| _route_after_eval | 条件路由逻辑；eval_pass=False且attempts>=3时返回END；eval_pass=True时前往sandbox | 单元测试 |
| _route_after_sandbox | 条件路由逻辑；有chart_html时返回END；有error且attempts>=3时返回END；有error且attempts<3时返回pythonagent；既无chart_html又无error的未知状态处理 | 单元测试 |

### 1.7 Agent/服务常量层 (Agent & Service Constants)

**文件**: `app/agents/sql_agent.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| user_toolkit | 列表完整性验证(2项工具)、工具名称正确 | 单元测试 |

**文件**: `app/services/analyst_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| eval_progress | 初始值验证(idle/0/0)、状态转换逻辑(idle→running→done/error) | 单元测试 |
| eval_lock | 线程锁并发安全性、同时启动两个评估任务时第二个被拒绝 | 单元测试 |
| get_progress | 评估进度查询、dict copy返回 | 单元测试 |

---

## 二、集成测试 (Integration Tests)

### 2.1 LangChain链集成测试

**文件**: `app/chains/user_chains.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| intent_chain | 意图分类准确性、安全检测 | 集成测试(需真实/模拟LLM) |
| direct_chain | 直接回复生成、历史上下文 | 集成测试(需真实/模拟LLM) |
| wrap_chain | SQL结果包装、自然语言生成 | 集成测试(需真实/模拟LLM) |
| warning_chain | 警告回复生成 | 集成测试(需真实/模拟LLM) |

**文件**: `app/chains/admin_chains.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| admin_intent_chain | 管理员意图分类、安全拦截 | 集成测试(需真实/模拟LLM) |
| admin_warning_chain | 管理员警告回复生成 | 集成测试(需真实/模拟LLM) |

**文件**: `app/chains/chart_chains.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| chart_intent_chain | 绘图意图判断 | 集成测试(需真实/模拟LLM) |
| chart_not_chain | 非绘图请求回复 | 集成测试(需真实/模拟LLM) |
| python_chart_chain | Python代码生成、格式规范 | 集成测试(需真实/模拟LLM) |

**文件**: `app/chains/eval_chains.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| response_eval_chain | 对话质量评估、JSON输出 | 集成测试(需真实/模拟LLM) |
| code_eval_chain | 代码质量评估、JSON输出 | 集成测试(需真实/模拟LLM) |

### 2.2 Agent集成测试

**文件**: `app/agents/sql_agent.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| sql_agent | Agent初始化、提示词配置 | 集成测试 |
| sql_executor | 工具调用链、最大迭代限制、错误处理 | 集成测试(需真实/模拟LLM+数据库) |

**文件**: `app/agents/admin_agent.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| admin_agent | Agent初始化、提示词配置 | 集成测试 |
| admin_executor | 工具调用链、批量操作、回滚触发 | 集成测试(需真实/模拟LLM+数据库) |

### 2.3 工作流集成测试

**文件**: `app/workflows/chart_workflow.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| _node_sqlagent | SQL查询节点执行 | 集成测试(需真实/模拟Agent) |
| _node_pythonagent | 代码生成节点执行 | 集成测试(需真实/模拟LLM) |
| _node_eval | 代码评估节点执行 | 集成测试 |
| _node_pyecharts_sandbox | Docker沙箱执行、HTML提取 | 集成测试(需Docker环境) |
| _build_chart_graph | 图拓扑结构验证、节点注册(4个)、边连接关系、入口点设置、条件路由配置 | 集成测试 |
| chart_graph | 完整工作流编排、节点流转 | 集成测试(需完整环境) |

### 2.4 服务层集成测试

**文件**: `app/services/user_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| direct_reply_stream | 直接回复流、历史保存、日志记录 | 集成测试(需真实/模拟LLM+数据库) |
| sql_query_stream | SQL查询流、Agent调用、结果包装 | 集成测试(需真实/模拟LLM+数据库) |
| warning_stream | 警告流、安全日志记录 | 集成测试(需真实/模拟LLM+数据库) |
| ai_stream | 意图路由、流式响应组装 | 集成测试(需真实/模拟LLM+数据库) |

**文件**: `app/services/admin_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| admin_warning_stream | 管理员警告流、安全日志 | 集成测试(需真实/模拟LLM+数据库) |
| admin_ai_stream | 管理员意图路由、Agent调用、流式响应 | 集成测试(需真实/模拟LLM+数据库) |

**文件**: `app/services/chart_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| chart_generate | 意图判断、工作流调用、流式响应 | 集成测试(需真实/模拟LLM+Docker) |

**文件**: `app/services/analyst_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| get_analyst_db_connection | 分析师数据库连接 | 集成测试(需数据库) |
| save_eval_result | 评估结果保存 | 集成测试(需数据库) |
| eval_one | 单条记录评估、结果保存 | 集成测试(需真实/模拟LLM+数据库) |
| evaluate_records_task_async | 异步批量评估执行、Semaphore(5)并发控制、异常不影响其他任务、完成后状态更新为done | 集成测试(需真实/模拟LLM+数据库) |
| start_evaluation | 批量评估任务启动、进度管理；已有任务运行时再次启动拒绝；空记录集处理 | 集成测试(需真实/模拟LLM+数据库) |
| query_results | 评估结果查询 | 集成测试(需数据库) |
| get_results_stats | 统计数据生成 | 集成测试(需数据库) |

---

## 三、端到端测试 (E2E Tests)

### 3.1 用户对话接口

**文件**: `app/routers/user.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| POST /api/ai/stream | 完整对话流程、流式响应格式 | E2E测试 |

**场景覆盖**:
- 正常对话请求 → 直接回复
- 需要SQL查询的请求 → 数据库查询 → 结果包装回复
- 安全威胁请求 → 警告拦截 → 安全日志记录
- 多轮对话 → 历史上下文保持
- 异常处理 → 错误流式返回

### 3.2 管理员对话接口

**文件**: `app/routers/admin.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| POST /api/admin/ai/stream | 完整管理员对话流程 | E2E测试 |

**场景覆盖**:
- 正常管理操作 → Agent工具调用 → 结果返回
- 批量操作 → 批次创建 → 多操作执行
- 回滚操作 → 数据恢复
- 安全威胁请求 → 警告拦截
- 危险SQL拦截 → 安全检测触发

### 3.3 图表生成接口

**文件**: `app/routers/chart.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| POST /api/chart/generate | 完整图表生成流程 | E2E测试 |

**场景覆盖**:
- 非图表请求 → 友好提示
- 图表请求 → SQL查询 → 代码生成 → 沙箱执行 → HTML返回
- 代码生成失败 → 重试机制 → 最大次数限制
- 沙箱执行失败 → 错误处理 → 日志记录
- 安全代码检测 → 危险代码拦截

### 3.4 质量评估接口

**文件**: `app/routers/analyst.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| POST /api/analyst/evaluate | 启动评估任务 | E2E测试 |
| GET /api/analyst/evaluate/progress | 获取评估进度 | E2E测试 |
| POST /api/analyst/query | 查询评估结果 | E2E测试 |
| GET /api/analyst/results | 获取统计结果 | E2E测试 |

**场景覆盖**:
- 启动评估 → 异步处理 → 进度更新
- 多表评估 → 会话匹配 → 结果保存
- 进度查询 → 实时状态返回
- 结果查询 → 条件过滤 → 分页返回
- 统计分析 → 聚合计算 → 图表数据生成

---

## 四、测试分层总结

### 4.1 单元测试覆盖范围

**纯单元测试(无需外部依赖)**:
- `app/models.py` - 所有Pydantic模型验证
- `app/history.py` - 历史管理逻辑
- `app/tools/admin_tools.py` - DANGEROUS_KEYWORDS, check_sql_safety, set_current_admin_name, admin_tools
- `app/workflows/chart_workflow.py` - ChartGraphState, _extract_python_code_block, _static_eval, _route_after_eval, _route_after_sandbox
- `app/agents/sql_agent.py` - user_toolkit
- `app/services/analyst_service.py` - eval_progress, eval_lock, get_progress

**需Mock的单元测试**:
- `app/logs.py` - 所有日志函数(Mock数据库连接)
- `app/tools/sql_tools.py` - sql_db_query(Mock数据库)
- `app/tools/admin_tools.py` - backup_data, create_user, safe_execute_sql, rollback_batch(Mock数据库)
- `app/tools/web_tools.py` - run_agent_command, baike_search_tool(Mock subprocess)
- `app/config.py` - 所有配置项含MODEL_NAME(Mock环境变量)

### 4.2 集成测试覆盖范围

**链层集成(需LLM)**:
- `app/chains/*.py` - 所有Chain的提示词效果、输出格式

**Agent集成(需LLM+数据库)**:
- `app/agents/sql_agent.py` - Agent工具选择、ReAct循环
- `app/agents/admin_agent.py` - 管理员Agent决策、工具调用

**工作流集成(需完整环境)**:
- `app/workflows/chart_workflow.py` - LangGraph状态流转、节点编排、图拓扑构建

**服务层集成(需LLM+数据库+Docker)**:
- `app/services/*.py` - 业务逻辑编排、多组件协调、异步并发评估

### 4.3 E2E测试覆盖范围

**API接口完整流程(需完整系统)**:
- `app/routers/*.py` - 从HTTP请求到响应的完整链路
- 包含：请求验证 → 服务调用 → 外部依赖(LLM/数据库/Docker) → 流式响应

---

## 五、测试依赖矩阵

| 组件 | 单元测试依赖 | 集成测试依赖 | E2E测试依赖 |
|-----|-------------|-------------|------------|
| models | 无 | 无 | 无 |
| history | 无 | 无 | 无 |
| logs | Mock数据库 | 真实数据库 | 真实数据库 |
| config | Mock环境变量 | 真实环境变量 | 真实环境变量 |
| tools | Mock数据库/subprocess | 真实数据库/subprocess | 真实数据库/subprocess |
| chains | Mock LLM | 真实/模拟 LLM | 真实 LLM |
| agents | 无(常量) | 真实/模拟 LLM+数据库 | 真实 LLM+数据库 |
| workflows | 无(纯函数) | 真实/模拟 LLM+Docker | 真实 LLM+Docker |
| services | 无(常量/锁) | 真实/模拟 LLM+数据库+Docker | 真实 LLM+数据库+Docker |
| routers | Mock服务层 | 真实服务层 | 完整系统 |

---

## 六、测试优先级建议

### P0 - 核心功能(必须测试)
1. 单元测试: models, history, logs, tools安全检测(DANGEROUS_KEYWORDS, check_sql_safety), workflow纯函数
2. 集成测试: user_chains意图路由, sql_agent工具调用
3. E2E测试: /api/ai/stream 正常对话流程

### P1 - 重要功能(建议测试)
1. 单元测试: config, tools业务逻辑, agent/service常量(user_toolkit, admin_tools, eval_progress, eval_lock)
2. 集成测试: admin_chains, chart_chains, admin_agent, _build_chart_graph, evaluate_records_task_async
3. E2E测试: /api/admin/ai/stream, /api/chart/generate

### P2 - 辅助功能(可选测试)
1. 单元测试: 所有工具函数, ChartGraphState, get_progress
2. 集成测试: eval_chains, analyst_service其他函数
3. E2E测试: /api/analyst/* 评估接口

---

## 七、测试数量统计

| 测试层级 | 数量 |
|---------|------|
| 单元测试 | 45 |
| 集成测试 | 35 |
| E2E接口 | 7 |
| E2E场景 | 20 |
| **测试对象合计** | **87** |
