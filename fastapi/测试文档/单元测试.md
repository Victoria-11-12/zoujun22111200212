# 单元测试 (Unit Tests)

## 1.1 数据模型层 (Models)

**文件**: `app/models.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| ChatRequest | 字段验证、默认值、必填校验 | 单元测试 |
| AdminChatRequest | 字段验证、默认值、必填校验 | 单元测试 |
| ChartRequest | 字段验证、默认值、必填校验 | 单元测试 |
| EvaluateRequest | 字段验证、默认值 | 单元测试 |
| EvalQueryRequest | 字段验证、可选字段处理 | 单元测试 |
| ResponseEvalResult | 字段范围验证(1-5分)、类型检查 | 单元测试 |
| CodeEvalResult | 字段范围验证(1-5分)、类型检查 | 单元测试 |

## 1.2 工具函数层 (Tools)

**文件**: `app/tools/sql_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| sql_db_query | SQL查询执行、参数传递 | 单元测试(需Mock数据库) |

**文件**: `app/tools/admin_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| DANGEROUS_KEYWORDS | 列表非空、正则可编译、覆盖DDL关键词和注入模式 | 单元测试 |
| check_sql_safety | 危险关键词检测、正则匹配；注释符`--`在SQL中间位置检测；分号后跟关键字`;\s*\w`匹配；大小写混合绕过尝试；安全SQL应通过 | 单元测试 |
| backup_data | 数据备份逻辑、JSON序列化 | 单元测试(需Mock数据库) |
| create_user | 用户创建、密码加密、重复检测 | 单元测试(需Mock数据库) |
| safe_execute_sql | SQL执行、安全拦截、备份触发；SELECT结果为空时返回值；UPDATE/DELETE无WHERE子句时行为；表名/条件正则匹配失败时行为 | 单元测试(需Mock数据库) |
| start_batch | 批次ID生成、全局变量设置 | 单元测试 |
| rollback_batch | 回滚逻辑、数据恢复；无可回滚记录时返回；混合INSERT/UPDATE/DELETE操作回滚顺序；回滚日志清理验证 | 单元测试(需Mock数据库) |
| set_current_admin_name | 全局变量设置 | 单元测试 |
| admin_tools | 列表完整性验证(4项工具)、工具名称正确 | 单元测试 |

**文件**: `app/tools/web_tools.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| run_agent_command | 子进程调用、超时处理、输出解析 | 单元测试(需Mock subprocess) |
| baike_search_tool | 输入校验、命令组装、结果解析 | 单元测试(需Mock子进程) |

## 1.3 历史管理模块 (History)

**文件**: `app/history.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| conversation_history | 全局字典初始化 | 单元测试 |
| MAX_HISTORY | 常量值验证 | 单元测试 |
| get_history | 存在/不存在会话ID的处理 | 单元测试 |
| save_history | 消息追加、历史截断、LangChain消息封装；恰好等于MAX_HISTORY*2时不截断；超过1条时截断至MAX_HISTORY*2；空字符串消息处理 | 单元测试 |

## 1.4 日志记录模块 (Logs)

**文件**: `app/logs.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| get_db_connection | 连接池获取、上下文管理器 | 单元测试(需Mock连接池) |
| log_user_chat | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_admin_chat | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_chart_generation | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |
| log_security_warning | SQL构造、参数传递、异常处理 | 单元测试(需Mock数据库) |

## 1.5 配置模块 (Config)

**文件**: `app/config.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| llm | ChatOpenAI初始化参数 | 单元测试(需Mock环境变量) |
| db | SQLDatabase初始化 | 单元测试(需Mock环境变量) |
| db_user | 只读数据库初始化 | 单元测试(需Mock环境变量) |
| engine | 连接池参数配置 | 单元测试(需Mock环境变量) |
| engine_readonly | 只读连接池配置 | 单元测试(需Mock环境变量) |
| eval_llm | 评估专用LLM初始化 | 单元测试(需Mock环境变量) |
| engine_analyst | 分析师连接池配置 | 单元测试(需Mock环境变量) |
| MODEL_NAME | 环境变量读取、日志依赖验证 | 单元测试(需Mock环境变量) |

## 1.6 工作流纯函数层 (Workflow Pure Functions)

**文件**: `app/workflows/chart_workflow.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| ChartGraphState | 状态定义、类型检查 | 单元测试 |
| _extract_python_code_block | 代码提取正则；无markdown标记时回退到完整文本；多个代码块时只取第一个 | 单元测试 |
| _static_eval | 静态代码检查、安全检测；`from pyecharts.xxx import yyy`形式检测；banned_tokens部分匹配误判；合法pyecharts代码应通过 | 单元测试 |
| _route_after_eval | 条件路由逻辑；eval_pass=False且attempts>=3时返回END；eval_pass=True时前往sandbox | 单元测试 |
| _route_after_sandbox | 条件路由逻辑；有chart_html时返回END；有error且attempts>=3时返回END；有error且attempts<3时返回pythonagent；既无chart_html又无error的未知状态处理 | 单元测试 |

## 1.7 Agent/服务常量层 (Agent & Service Constants)

**文件**: `app/agents/sql_agent.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| user_toolkit | 列表完整性验证(2项工具)、工具名称正确 | 单元测试 |

**文件**: `app/services/analyst_service.py`

| 测试对象 | 测试内容 | 测试类型 |
|---------|---------|---------|
| eval_progress | 初始值验证(idle/0/0)、状态转换逻辑(idle→running→done/error) | 单元测试 |
| eval_lock | 线程锁并发安全性、同时启动两个评估任务时第二个被拒绝 | 单元测试 |
| get_progress | 评估进度查询、dict copy返回 | 单元测试 |

---

## 单元测试覆盖范围

**纯单元测试(无需外部依赖)**:
- `app/models.py` - 所有Pydantic模型验证
- `app/history.py` - 历史管理逻辑
- `app/tools/admin_tools.py` - DANGEROUS_KEYWORDS, check_sql_safety, set_current_admin_name, admin_tools
- `app/workflows/chart_workflow.py` - ChartGraphState, _extract_python_code_block, _static_eval, _route_after_eval, _route_after_sandbox
- `app/agents/sql_agent.py` - user_toolkit
- `app/services/analyst_service.py` - eval_progress, eval_lock, get_progress

**需Mock的单元测试**:
- `app/logs.py` - 所有日志函数(Mock数据库连接)
- `app/tools/sql_tools.py` - sql_db_query(Mock数据库)
- `app/tools/admin_tools.py` - backup_data, create_user, safe_execute_sql, rollback_batch(Mock数据库)
- `app/tools/web_tools.py` - run_agent_command, baike_search_tool(Mock subprocess)
- `app/config.py` - 所有配置项含MODEL_NAME(Mock环境变量)

---

## 测试优先级建议

### P0 - 核心功能(必须测试)
1. 单元测试: models, history, logs, tools安全检测(DANGEROUS_KEYWORDS, check_sql_safety), workflow纯函数

### P1 - 重要功能(建议测试)
1. 单元测试: config, tools业务逻辑, agent/service常量(user_toolkit, admin_tools, eval_progress, eval_lock)

### P2 - 辅助功能(可选测试)
1. 单元测试: 所有工具函数, ChartGraphState, get_progress

---

## 测试数量统计

| 测试层级 | 数量 |
|---------|------|
| 单元测试 | 45 |
