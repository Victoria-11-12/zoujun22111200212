# 2026-04-02

## 新增功能

### LightGBM 模型集成
- 将训练好的 LightGBM 模型集成到票房预测系统
- 黑马筛选接口改用 LightGBM 模型进行票房预测
- 支持实时预测和 ROI 计算
- 特征工程：使用 `genres`、`New_Director`、`New_Actor`、`budget` 四个特征
- 特征自动编码：类别特征自动映射为数值

### 缓存优化

**实现原理：**
- 使用内存字典存储预测结果，包含数据、时间戳、过期时间
- 基于时间戳的过期机制（TTL - Time To Live）
- 请求时先检查缓存有效性，有效则直接返回，无效则重新计算

**技术细节：**
```python
dark_horses_cache = {
    'data': None,              # 缓存的预测结果
    'timestamp': 0,            # 缓存生成时间（秒级时间戳）
    'expire_seconds': 300      # 缓存有效期：5 分钟
}

# 缓存检查逻辑
current_time = time.time()
if (cache['data'] is not None and 
    current_time - cache['timestamp'] < cache['expire_seconds']):
    return cache['data']  # 缓存命中
else:
    # 重新计算并更新缓存
```

**性能提升：**
- 首次请求：完整计算（约 2-5 秒）
- 后续请求：直接返回缓存（< 100 毫秒）
- 性能提升约 20-50 倍

**适用场景：**
- 数据更新频率低（电影数据相对稳定）
- 计算成本高（7598 条数据的特征工程 + 模型预测）
- 多用户并发访问

## 优化改进

### 数据展示优化
- 调整票房显示格式：从 M 单位改为完整美元金额
- 表格数据更直观，避免小数位过多
- 返回字段优化：`movie_title`、`budget`、`predicted_gross`、`predicted_roi`、`genres`

### 数据处理
- 改进数据清洗逻辑，过滤 budget 为空的记录
- 自动处理缺失值和异常值
- 支持特征自动对齐和编码
- New_Director/New_Actor 特征：基于 Facebook 点赞数自动判断（0 或空值为 No，否则为 Yes）

## 问题修复
- 修复模型预测值尺度转换问题（对数→原始）
- 修复空数据导致的筛选异常
- 修复编码器缺失问题：改用手动编码映射，避免依赖外部编码器文件
- 修复预算数据缺失问题：自动过滤 budget 为空的记录（7598 条数据中保留 4458 条有效记录）
- 修复特征列缺失错误：分离特征 DataFrame 和原始 DataFrame，保留展示字段

## 技术细节

### 特征工程
```python
# New_Director 判断
df['New_Director'] = df['director_facebook_likes'].apply(
    lambda x: 'No' if pd.isna(x) or float(x) == 0 else 'Yes'
)

# New_Actor 判断
df['New_Actor'] = df['actor_1_facebook_likes'].apply(
    lambda x: 'No' if pd.isna(x) or float(x) == 0 else 'Yes'
)

# 类别特征编码
df['New_Director'] = df['New_Director'].apply(lambda x: 1 if x == 'yes' else 0)
df['genres'] = df['genres'].apply(lambda x: hash(str(x)) % 2)
```

### 模型预测流程
1. 从 Node API 获取电影数据（7598 条）
2. 过滤 budget 为空的记录（保留 4458 条）
3. 特征工程和编码
4. LightGBM 模型预测票房
5. 计算 ROI = predicted_gross / budget
6. 按 ROI 排序返回前 30 条黑马影片
